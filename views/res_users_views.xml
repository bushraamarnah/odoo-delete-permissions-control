<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_hide_delete_inherit" model="ir.ui.view">
        <field name="name">res.users.form.hide.delete.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='access_rights']" position="inside">
                <group string="Delete Permissions Control">
                    <field name="hide_delete_all"/>
                    <field name="delete_restriction_ids"
                           widget="many2many_tags"
                           options="{'no_create': True}"
                           invisible="hide_delete_all == True"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>